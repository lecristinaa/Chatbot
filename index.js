// Fazendo a pergunta
const pergunta = "Qual país você mora?"

// Verificando se é uma pergunta
if(pergunta.endsWith("?")){

    // Separa as palavras por espaço
    console.log(pergunta.split(" "))
}

// Dar resposta (mudar sujeitos e verbo)


// Embelezar resposta
